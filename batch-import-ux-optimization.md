# 批量导入页面体验优化方案（PRD / 交互优化）

> **文档状态**：方案稿（仅设计，不涉及代码实现）
> **关联模块**：`src/App.vue` 中的 `parseBatch` / `submitBatch`（当前版本）
> **目标读者**：产品经理 / 前端 / 测试
> **核心目标**：把"能用但让人提心吊胆"的批量导入，升级为"敢用、出错可定位、可修正重传"的稳健流程。

---

## 0. 背景与目标

当前批量导入入口在 `App.vue` 内，实现形态为：**一个 textarea + 一行 `batchMsg` 文本提示 + 一个"确认提交"按钮**。

- 解析、校验、进度、成败全部挤进 `batchMsg` 单行文本；
- 提交时遇到第一篇异常即 `return`，后续作品状态完全未知；
- 无模板、无样例、无分步、无进度条、无错误导出。

**本次优化目标（可衡量）**：

1. 导入成功率可预期——提交前用户看清"能进几条 / 会丢几条"；
2. 错误可定位、可修正、可重传——不丢原始输入，支持导出错误行；
3. 大批量有进度感知——进度条 + 已处理/总数 + 当前篇名；
4. 单篇失败不阻断整批——结束统一汇总，而非碰第一个就黑屏。

---

## 一、现状问题分析

基于 `App.vue` 现有 `parseBatch` / `submitBatch` 实现梳理：

| 维度 | 具体病灶 | 用户感受 |
|------|---------|---------|
| 反馈通道单一 | 解析结果、错误、进度（`提交中… X/N`）、成败全塞进 `batchMsg` 单行文本 | "它到底在干嘛？我看不出来重点" |
| 失败即中断 | `submitBatch` 的 `catch` 命中首篇异常直接 `return`，后续作品状态未知 | "第 3 篇失败，那 4~10 篇呢？" |
| 零引导 | 无模板下载、无"看样例"入口，只能靠用户自己知道"网h系列导航"格式 | "我该粘成啥样？" |
| 无分步心智 | 粘贴、预览、确认、结果堆同一屏，无"先做什么后做什么"的视觉锚点 | "这是一步还是四步？" |
| 错误无出口 | 仅 `invalid` 篇在预览里"标红"，无法批量导出修正后重传 | "标红了然后呢？我得手动补" |
| 进度感知弱 | 纯文本计数，无进度条；100+ 篇时不知卡在哪、还要多久 | "到底传完没？" |
| 边界全空白 | 无条数 / 大小 / 格式限制与前置校验 | 超量直接卡死或抛异常 |
| 可重复提交 | 提交中"确认提交"仅按 `preview.length` 禁用，提交进行中 preview 仍在，**按钮仍可被点** | 重复写入风险 |
| 视觉/移动端 | 输入框与按钮混排，`.preview-item` 标题/链接/状态平铺，窄屏体验差 | 小屏几乎不可用 |

---

## 二、信息架构优化

重划为 **6 个清晰区域**，自上而下排布（窄屏可折叠为手风琴）：

```
┌─────────────────────────────────────────────┐
│ ① 导入入口与引导区                             │
│   "把多篇作品一次塞进来" + [下载模板] [看样例]   │
├─────────────────────────────────────────────┤
│ ② 输入区（Tab 切换）                           │
│   [ 粘贴文本 ] [ 上传文件 .txt/.csv/.md ]       │
├─────────────────────────────────────────────┤
│ ③ 模板与样例区（可折叠）                        │
│   标准格式片段 + 字段说明                       │
├─────────────────────────────────────────────┤
│ ④ 解析校验结果区                               │
│   摘要：有效 X / 警告 Y / 错误 Z               │
│   结构化预览表：标题 | 链接数 | 状态徽标        │
├─────────────────────────────────────────────┤
│ ⑤ 错误处理与修正区                             │
│   行级错误清单 + [导出错误行] [内联编辑]        │
├─────────────────────────────────────────────┤
│ ⑥ 进度与汇总区                               │
│   进度条 + 已处理/总数 + 成功/跳过/失败汇总     │
└─────────────────────────────────────────────┘
```

**区域职责边界**

- **① 引导区**：降低首因成本，回答"这是什么、怎么用"。
- **② 输入区**：统一管线——粘贴或上传，最终都进入同一解析器。
- **③ 样例区**：把隐性格式知识显性化，减少解析失败。
- **④ 结果区**：给用户"提交前全景"，校验摘要常驻。
- **⑤ 修正区**：错误闭环的核心，提供导出 / 内联两条修正路径。
- **⑥ 汇总区**：过程进度 + 终态回顾，不清空原始输入。

---

## 三、交互流程改进

### 3.1 状态机

```
[选择方式] ──输入──▶ [解析校验] ──确认──▶ [确认导入] ──完成──▶ [结果汇总]
   idle            parsing          preview          submitting        done
                    │                                  │
                    │ 解析=0 条                         │ 单篇异常
                    ▼                                  ▼
               [回退引导区]                     计入失败列表，继续后续
                                                 （不中断，结束统一汇总）
```

### 3.2 各状态用户反馈

| 状态 | 界面表现 | 反馈机制 |
|------|---------|---------|
| `idle` | 引导区可见，输入区空态有友好占位（"先粘点东西，或下载模板"） | 空态文案 |
| `parsing` | "解析中"按钮 loading，禁用二次触发；解析=0 时回退引导区并给样例深链 | loading + 空结果引导 |
| `preview` | 展示校验摘要 + 结构化预览表；确认按钮按"有效篇数"启用（0 有效则禁用） | 摘要数字 + 按钮态 |
| `submitting` | 进度条 + 当前篇名 + 已处理/总数；**锁死确认按钮**防重复提交 | 进度条 + 锁按钮 |
| `done/error` | 进入结果汇总，保留原始输入便于修正重传 | 汇总页（见第五节） |

**关键改动**：`submitBatch` 的"遇错即 `return`"改为"单篇异常计入失败列表、继续后续"，结束才给全景。确认按钮在 `submitting` 期间硬性禁用。

---

## 四、批量校验与错误提示

### 4.1 行级错误表

| 列 | 说明 |
|----|------|
| 行号 | 原始批次中的序号 |
| 标题 | 解析出的标题（若有） |
| 错误类型 | 见 4.2 分类 |
| 原始片段 | 出错那一行的原文，便于对照 |
| 操作 | [内联编辑] / [忽略] |

### 4.2 错误类型分类

- **缺标题**：无《》也无版本条目；
- **无有效链接**：只有 4–15 位短数字，被判定 `invalid`；
- **链接格式非法**：非 `http(s)`、或被 XSS 触发器拦截的伪协议（`javascript:`/`data:`/`vbscript:`）；
- **疑似重复**：与站内已有作品，或本次批次内标题/首链接重复。

### 4.3 修正闭环（两条路径并存）

- **导出修正**：把错误行导出为 CSV / Excel，用户本地改完重新上传；
- **内联编辑**：预览表里直接改标题 / 补链接，实时重校验。

> 校验摘要（有效 / 警告 / 错误）常驻，让用户提交前就看清"能进几条、会丢几条"。

---

## 五、进度与结果反馈

### 5.1 大批量进度

- 顶部**进度条（百分比）** + 「已处理 N / 共 M」+ 当前正在处理的篇名；
- 可选：按已完成篇均耗时推算"预计剩余时间"。

### 5.2 结果汇总页

- **成功 N 篇 / 跳过 M 篇（如重复）/ 失败 K 篇**三组数字并列；
- 失败列表可逐条「重试」或「忽略」；
- 提供「导出失败明细」供二次处理；
- **不清空原始输入**，便于直接修正后重传。

### 5.3 不中断策略

单篇失败不阻断整批，结束才给全景（与第三节一致）。

---

## 六、边界与约束

| 约束项 | 建议值 | 友好提示策略 |
|--------|--------|--------------|
| 输入方式 | 粘贴文本 / 上传 `.txt`·`.csv`·`.md` | 上传区标注支持格式与图标 |
| 单文件大小 | ≤ 2 MB | 超限即时拦截："文件超过 2MB，请拆分后上传" |
| 单次条数 | ≤ 200 篇 | 超量提示"建议分批，已为你保留前 200 条"或引导拆分 |
| 单篇链接数 | ≤ 50 | 超额截断并提示 |
| 空输入 | — | 点解析时若为空："先粘点东西进来呀（或下载模板）" |
| 解析 0 条 | — | 给格式样例 + 「看样例」深链，而非冷冰冰报错 |
| 全失败 | — | 汇总页引导"导出错误行修正后重传"，不丢原始输入 |
| 频控 | 导入间隔 / 总量阈值 | 防刷库，超限提示"稍后再试" |

---

## 附 A：原型骨架（文字线框）

```
┌───────────────────────────────────────────────┐
│  批量导入                               [关闭 ✕] │
├───────────────────────────────────────────────┤
│  把多篇作品一次塞进来。                          │
│  [⬇ 下载模板]   [👁 看样例]                      │
│                                                │
│  (●) 粘贴文本   ( ) 上传文件                     │
│  ┌─────────────────────────────────────────┐  │
│  │ 网h系列导航                               │  │
│  │ 《作品A》                                 │  │
│  │ pan.quark / 提取码 xxx                    │  │
│  │ 《作品B》                                 │  │
│  │ ...                                       │  │
│  └─────────────────────────────────────────┘  │
│  [ 解析 ]                                      │
│                                                │
│  ── 解析后 ─────────────────────────────────  │
│  有效 8 · 警告 1 · 错误 2                       │
│  ┌─────────────────────────────────────────┐  │
│  │ #1 《作品A》      3链接   ✅ 可导入        │  │
│  │ #2 《作品B》      2链接   ✅ 可导入        │  │
│  │ #9 （无标题）     1链接   ⚠ 缺标题 [编辑] │  │
│  │ #10（短数字）     0链接   ❌ 无有效链接    │  │
│  └─────────────────────────────────────────┘  │
│  [导出错误行]   [ 确认导入 8 篇 ]               │
│                                                │
│  ── 导入中 ─────────────────────────────────  │
│  [████████░░░░░░] 5/8  《作品F》…              │
│                                                │
│  ── 完成 ───────────────────────────────────  │
│  成功 7 · 跳过 1（重复）· 失败 0               │
│  [导出失败明细]   [ 再传一批 ]                  │
└───────────────────────────────────────────────┘
```

## 附 B：验收指标（上线后建议埋点 / 核对）

1. **解析成功率**：进入 `preview` 且有效篇数 > 0 的会话占比；
2. **首因失败率**：因"格式不对"解析=0 而回退的占比（目标下降）；
3. **错误修正闭环率**：触发错误的会话中，最终成功重传的比例；
4. **重复提交率**：提交进行中重复点击的拦截次数；
5. **大批量完成率**：≥50 篇会话的完整导入完成率。

---

## 附 C：与现有代码的映射（供后续实现参考）

| 方案要点 | 当前代码位置 | 改造方向 |
|---------|-------------|---------|
| 单一 `batchMsg` 文本 | `App.vue` `batchMsg` | 拆分为 6 个区域的状态组件 |
| 遇错即 `return` | `submitBatch` 的 `catch` | 改为失败列表 + 继续循环 |
| 无模板/样例 | 无 | 新增引导区 + 样例数据 |
| 无进度条 | `batchMsg` 文本计数 | `submitting` 进度条组件 |
| 无错误导出 | 无 | 新增 CSV/Excel 导出 |
| 可重复提交 | 按钮仅按 `preview.length` 禁用 | `submitting` 期间硬性禁用 |

> 落地状态：已于 commit `7e238d6` 实现并部署（状态机 batchState / 进度条 batchProgress / 错误与失败 CSV 导出 / 批次内去重跳过 / submitting 期间锁提交按钮）。`src/App.vue` 新增 `doParse` 状态驱动、`submitBatch` 不中断循环 + 失败列表；`src/style.css` 新增引导/样例/汇总/进度样式。
