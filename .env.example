# 复制本文件为 .env 并填入你的 CloudBase 环境 ID
# 留空也能跑：会自动用浏览器本地存储(localStorage)演示，数据只存在你这台电脑
#
# 去 CloudBase 控制台（https://console.cloud.tencent.com/tcb）→ 你的环境 →
# 概览里复制「环境 ID」（形如 fan-library-7g3k2q9p），填到下面：
VITE_CLOUDBASE_ENV_ID=

# 说明：
#   • envId 是「公开标识符」不是密钥，嵌在前端代码里没问题（安全靠数据库安全规则）
#   • 部署到 Netlify 时，把这条加到 Site settings → Environment variables 里即可
#   • 数据库「安全规则」需设为允许匿名读 + 匿名写（无审核投稿场景），见 cloudbase/security-rules.json
